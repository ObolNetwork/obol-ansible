---
- name: Include Docker deployment tasks
  ansible.builtin.include_tasks: docker.yml
  when: deployment_mode | default('docker') == 'docker'

- name: Include bare-metal deployment tasks
  ansible.builtin.include_tasks: baremetal.yml
  when: deployment_mode | default('docker') == 'baremetal'
